# Phoneme ASR Pipeline

## Environment

Install dependencies with Pixi:

pixi install

## System dependency

This project requires espeak-ng.

macOS:
brew install espeak-ng

Ubuntu:
sudo apt install espeak-ng

## Run pipeline

pixi shell
dvc repro